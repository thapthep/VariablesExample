public class algorithm {
    public static void main(String[] args) {
        int radius = 5; // Radius of the circle
        int area = (int) (Math.PI * radius * radius); // Area of the circle
        System.out.println("Area of circle that has radius = " + radius + " is: " + area);
    }
    
}
